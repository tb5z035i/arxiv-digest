## Cursor Cloud specific instructions

This repository is currently empty (only a blank `README.md`). There are no services, dependencies, build steps, or applications to run.

When code is added to the repository, update this section with:
- How to install dependencies
- How to run lint, tests, and the dev server
- Any non-obvious caveats discovered during setup
